enum Endpoints { items, details }

enum PlatformCheck { isIos, isAndroid }

enum InfoUserLoading { initial, loading, success, failure }

enum NavBarItems { home, explorer, profile, notification, setting }

enum StateIdle { loading, error, success }
